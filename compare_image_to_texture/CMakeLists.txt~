cmake_minimum_required(VERSION 2.8 FATAL_ERROR)

project(resize_original)

find_package( OpenCV REQUIRED )

add_executable (resize_original resize_original.cpp)
target_link_libraries (resize_original ${OpenCV_LIBS} )

